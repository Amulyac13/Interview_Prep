/** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

const nextConfig = {
  images: {
    domains: ['img.freepik.com', 'res.cloudinary.com','cdn-icons-png.flaticon.com',],
  },
};

export default nextConfig;